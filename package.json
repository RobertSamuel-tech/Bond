{
  "name": "bond-service",
  "version": "1.0.0",
  "description": "BOND — Agent Stake & Slash Service. A REST API where AI agents post credibility stakes that other agents can slash for misbehavior.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test \"tests/*.test.js\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "better-sqlite3": "^12.2.0"
  }
}
